import * as path from 'path';

export default {
  routesPath: path.join(__dirname, 'routes'),
  wxToken: 'wechat-amdin backend-api',
  port: 9000,
  routesPath: path.join(__dirname, 'routes'),
  wxToken: 'hello,wx'
};
