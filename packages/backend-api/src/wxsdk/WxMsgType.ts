export enum WxMsgType {
  /**
   * 文本消息（收发）
   */
  text = 'text',
  /**
   * 图片消息（收发）
   */
  image = 'image',
  /**
   * 语音消息（收发）
   */
  voice = 'voice',
  /**
   * 视频消息（收发）
   */
  video = 'video',
  /**
   * 音乐消息（发）
   */
  music = 'music',
  /**
   * 图文消息（发）
   */
  news = 'news',
  /**
   * 小视频消息（收）
   */
  shortvideo = 'shortvideo',
  /**
   * 地理位置消息（收）
   */
  location = 'location',
  /**
   * 链接消息（收）
   */
  link = 'link',
  /**
   * 事件推送（收）
   */
  event = 'event'
}
